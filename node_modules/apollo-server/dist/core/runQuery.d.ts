import { GraphQLSchema, GraphQLResult, Document } from 'graphql';
export interface GqlResponse {
    data?: Object;
    errors?: Array<string>;
}
declare function runQuery(schema: GraphQLSchema, query: string | Document, rootValue?: any, context?: any, variables?: {
    [key: string]: any;
}, operationName?: string): Promise<GraphQLResult>;
export { runQuery };
